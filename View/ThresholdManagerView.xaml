<UserControl x:Class="JobReporter2.View.ThresholdManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JobReporter2.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding Thresholds}" AutoGenerateColumns="False" CanUserAddRows="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding Enabled}" />
                <DataGridTextColumn Header="Value1" Binding="{Binding Value1}" />
                <DataGridTextColumn Header="Value2" Binding="{Binding Value2}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
