<UserControl x:Class="JobReporter2.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:JobReporter2.View">
    <Grid>
        <!-- Layout -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Header row -->
            <RowDefinition Height="*" />
            <!-- Main content -->
            <RowDefinition Height="20" />
            <!-- Status bar -->
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,0">
			<StackPanel Orientation="Vertical">
				<Button Content="Edit Filter" Command="{Binding OpenFilterCommand}" Margin="0,0,10,0" />
				<Button Content="Generate Report" Command="{Binding GenerateReportCommand}" Margin="0,10,10,0" />
			</StackPanel>
            <TextBox Text="{Binding SelectedFilter}" IsReadOnly="True" Width="400" />
        </StackPanel>
        
        <TabControl Grid.Row="1" Margin="10 10 10 10">
            <!-- Jobs Tab -->
            <TabItem Header="Jobs">
                <local:JobsContent/>
            </TabItem>

            <!-- Reports Tab -->
            <TabItem Header="Reports">
                <!-- Placeholder for future content -->
                <TextBlock Text="Reports content goes here" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <Grid Grid.Row="2" Background="{StaticResource StatusBarBackground}" >
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Records Loaded:" Margin="0,0,10,0" Foreground="White" />
				<TextBlock Text="{Binding FilteredRecordCount}" Margin="0,0,10,0" Foreground="White" />
				<TextBlock Text="/" Margin="0,0,10,0" Foreground="White" />
                <TextBlock Text="{Binding RecordCount}" Margin="0,0,10,0" Foreground="White" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>

