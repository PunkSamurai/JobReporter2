<UserControl x:Class="JobReporter2.View.JobsContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Main Content Grid -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header -->
                <RowDefinition Height="*"/>
                <!-- DataGrid -->
            </Grid.RowDefinitions>

            <!-- Header panel -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,0">
                <StackPanel Orientation="Vertical">
                    <Button Content="Edit Filters" Command="{Binding OpenFilterCommand}" Margin="0,0,10,0" />
                    <Button Content="Clear Filters" Command="{Binding ClearFilterCommand}" Margin="0,10,10,0" />
                </StackPanel>
                <TextBox Text="{Binding SelectedFilter}" IsReadOnly="True" Width="400" TextWrapping="Wrap" AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" />
                <Button Content="Manage Shifts" Command="{Binding OpenShiftManagerCommand}" Width="120" Margin="5"/>
                <StackPanel Orientation="Vertical">
                    <ComboBox Width="200" Margin="5"
                             ItemsSource="{Binding ReportTypes}"
                             SelectedItem="{Binding SelectedReportType}"
                             Text="Select Report Type" />
                    <ComboBox Width="200" Margin="5"
                             ItemsSource="{Binding TimeFrames}"
                             SelectedItem="{Binding SelectedTimeFrame}"
                             Text="Select Time Frame" />
                </StackPanel>
                <Button Content="Generate Report" Command="{Binding GenerateReportCommand}" Margin="5" />
            </StackPanel>

            <!-- DataGrid -->
            <!-- DataGrid -->
            <DataGrid
                 x:Name="JobDataGrid"
                Grid.Row="1" 
          ItemsSource="{Binding FilteredJobs}" 
          SelectedItem="{Binding SelectedJob}" 
          AutoGenerateColumns="False"
          Margin="10"
          ScrollViewer.VerticalScrollBarVisibility="Auto"
          ScrollViewer.CanContentScroll="True">
                <DataGrid.Columns>
                    <!-- Connection Column -->
                    <DataGridTextColumn Header="Connection" Binding="{Binding Connection}" />

                    <!-- Name Column -->
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" />

                    <!-- Job File Column -->
                    <DataGridTextColumn Header="Job File" Binding="{Binding JobFile}" />

                    <!-- OEM String Column -->
                    <DataGridTextColumn Header="OEM Name" Binding="{Binding OEMString}" />

                    <!-- Shift Column -->
                    <DataGridTextColumn Header="Shift" Binding="{Binding Shift}" />

                    <!-- Start Type Column -->
                    <DataGridTextColumn Header="Start Type" Binding="{Binding StartType}" />

                    <!-- Prep Time Column -->
                    <DataGridTextColumn Header="Prep Time" Binding="{Binding PrepTime}" />

                    <!-- Start Time Column -->
                    <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime}" />

                    <!-- End Time Column -->
                    <DataGridTextColumn Header="End Time" Binding="{Binding EndTime}" />

                    <!-- Total Time Column -->
                    <DataGridTextColumn Header="Total Time" Binding="{Binding TotalTime}" />

                    <!-- Wasted Time Column -->
                    <DataGridTextColumn Header="Wasted Time" Binding="{Binding WastedTime}" />

                    <!-- Machine Time Column -->
                    <DataGridTextColumn Header="Machine Time" Binding="{Binding MachineTime}" />

                    <!-- Feedrate Override Column -->
                    <DataGridTextColumn Header="Feedrate Override" Binding="{Binding FeedrateOverride}" />

                    <!-- Slew Time Column -->
                    <DataGridTextColumn Header="Slew Time" Binding="{Binding SlewTime}" />

                    <!-- Pause Time Column -->
                    <DataGridTextColumn Header="Pause Time" Binding="{Binding PauseTime}" />

                    <!-- Sheet Count Column -->
                    <DataGridTextColumn Header="Sheet Count" Binding="{Binding SheetCount}" />

                    <!-- PD Count Column -->
                    <DataGridTextColumn Header="PD Count" Binding="{Binding PD_Count}" />

                    <!-- Material Column -->
                    <DataGridTextColumn Header="Material" Binding="{Binding Material}" />

                    <!-- Sheet Change Time Column -->
                    <DataGridTextColumn Header="Sheet Change Time" Binding="{Binding SheetChangeTime}" />

                    <!-- Tools Column -->
                    <DataGridTextColumn Header="Tools" Binding="{Binding Tools}" />

                    <!-- Tool Change Time Column -->
                    <DataGridTextColumn Header="Tool Change Time" Binding="{Binding ToolChangeTime}" />

                    <!-- Tool Avg Times Column -->
                    <DataGridTextColumn Header="Tool Avg Times" Binding="{Binding ToolAvgTimes}" />

                    <!-- Size Column -->
                    <DataGridTextColumn Header="Size" Binding="{Binding Size}" />

                    <!-- Flagged Column -->
                    <DataGridCheckBoxColumn Header="Flagged" Binding="{Binding Flagged}" />

                    <!-- End Type Column with Conditional Formatting -->
                    <DataGridTemplateColumn Header="End Type">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding EndType}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <!-- Green background for 'Job Completed' -->
                                                <DataTrigger Binding="{Binding EndType}" Value="Job completed.">
                                                    <Setter Property="Background" Value="LightGreen" />
                                                </DataTrigger>
                                                <!-- Red background for 'Job Halted' -->
                                                <DataTrigger Binding="{Binding EndType}" Value="Job halted. Cancelled by operator.">
                                                    <Setter Property="Background" Value="LightCoral" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Side Panel for Selected Job Details -->
        <Expander Grid.Column="1" Grid.Row="0" Margin="10" IsExpanded="False" ExpandDirection="Left" MaxWidth="320">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <!-- Label column -->
                        <ColumnDefinition Width="*" />
                        <!-- Value column -->
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Labels and Values -->
                    <TextBlock Grid.Column="0" Grid.Row="0" FontWeight="Bold" Text="Connection:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding SelectedJob.Connection}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="1" FontWeight="Bold" Text="Job File:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding SelectedJob.JobFile}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="2" FontWeight="Bold" Text="End Type:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding SelectedJob.EndType}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="3" FontWeight="Bold" Text="Prep Time:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="3" Text="{Binding SelectedJob.PrepTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="4" FontWeight="Bold" Text="Start Time:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="4" Text="{Binding SelectedJob.StartTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="5" FontWeight="Bold" Text="Total Time:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="5" Text="{Binding SelectedJob.TotalTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="6" FontWeight="Bold" Text="OEM String:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="6" Text="{Binding SelectedJob.OEMString}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="7" FontWeight="Bold" Text="Connection Name:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="7" Text="{Binding SelectedJob.ConnectionName}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="8" FontWeight="Bold" Text="Shift:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="8" Text="{Binding SelectedJob.Shift}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="9" FontWeight="Bold" Text="Feedrate Override %:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="9" Text="{Binding SelectedJob.FeedrateOverride}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="10" FontWeight="Bold" Text="Slew Time:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="10" Text="{Binding SelectedJob.SlewTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="11" FontWeight="Bold" Text="Pause Time:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="11" Text="{Binding SelectedJob.PauseTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="12" FontWeight="Bold" Text="Sheet Count:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="12" Text="{Binding SelectedJob.SheetCount}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="13" FontWeight="Bold" Text="Sheet Change Time:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="13" Text="{Binding SelectedJob.SheetChangeTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="14" FontWeight="Bold" Text="Tools:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="14" Text="{Binding SelectedJob.Tools}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="15" FontWeight="Bold" Text="Tool Change Time:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="15" Text="{Binding SelectedJob.ToolChangeTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="16" FontWeight="Bold" Text="Tool Avg Times:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="16" Text="{Binding SelectedJob.ToolAvgTimes}" TextWrapping="Wrap" />
                </Grid>

                <oxy:PlotView Model="{Binding SelectedJob.PieChartModel}" 
                      Height="300" 
                      Width="300" 
                      Margin="10" />
            </StackPanel>
        </Expander>



    </Grid>
</UserControl>