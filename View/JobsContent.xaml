<UserControl x:Class="JobReporter2.View.JobsContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Main Content Grid -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header -->
                <RowDefinition Height="*"/>
                <!-- DataGrid -->
            </Grid.RowDefinitions>

            <!-- Header panel -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,0">
                <StackPanel Orientation="Vertical">
                    <Button Content="Edit Filters" Command="{Binding OpenFilterCommand}" Margin="0,0,10,0" />
                    <Button Content="Clear Filters" Command="{Binding ClearFilterCommand}" Margin="0,10,10,0" />
                </StackPanel>
                <TextBox Text="{Binding SelectedFilter}" IsReadOnly="True" Width="400" TextWrapping="Wrap" AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" />
                <Button Content="Manage Shifts" Command="{Binding OpenShiftManagerCommand}" Width="120" Margin="5"/>
                <StackPanel Orientation="Vertical">
                    <ComboBox Width="200" Margin="5"
                             ItemsSource="{Binding ReportTypes}"
                             SelectedItem="{Binding SelectedReportType}"
                             Text="Select Report Type" />
                    <ComboBox Width="200" Margin="5"
                             ItemsSource="{Binding TimeFrames}"
                             SelectedItem="{Binding SelectedTimeFrame}"
                             Text="Select Time Frame" />
                </StackPanel>
                <Button Content="Generate Report" Command="{Binding GenerateReportCommand}" Margin="5" />
            </StackPanel>

            <!-- DataGrid -->
            <DataGrid
                x:Name="JobDataGrid"
                Grid.Row="1" 
                ItemsSource="{Binding FilteredJobs}" 
                SelectedItem="{Binding SelectedJob}" 
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                SelectionMode="Single"
                Margin="10"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.CanContentScroll="True"> 
                <DataGrid.Columns>
                    <!-- Flagged Column -->
                    <DataGridCheckBoxColumn Header="Flagged" Binding="{Binding Flagged}" IsReadOnly="False"/>

                    <!-- Connection Column -->
                    <DataGridTextColumn Header="Connection" Binding="{Binding Connection}" IsReadOnly="True"/>

                    <!-- OEM String Column -->
                    <DataGridTextColumn Header="OEM Name" Binding="{Binding OEMString}" IsReadOnly="True"/>
                    
                    <!-- Name Column -->
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True"/>

                    <!-- Size Column -->
                    <DataGridTextColumn Header="Size" Binding="{Binding Size}" IsReadOnly="True"/>

                    <!-- Shift Column -->
                    <DataGridTemplateColumn Header="Shift" SortMemberPath="Shift">
                        <DataGridTemplateColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Background" 
                                        Value="{Binding Shift, Converter={StaticResource ShiftToBrushConverter}}" />
                            </Style>
                        </DataGridTemplateColumn.CellStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Shift}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Start Type Column -->
                    <DataGridTextColumn Header="Start Type" Binding="{Binding StartType}" IsReadOnly="True"/>

                    <!-- End Type Column with Conditional Formatting -->
                    <DataGridTemplateColumn Header="End Type" SortMemberPath="EndType" IsReadOnly="True">
                        <DataGridTemplateColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Background"
                                    Value="{Binding EndType, Converter={StaticResource EndTypeToBrushConverter}}" />
                            </Style>
                        </DataGridTemplateColumn.CellStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding EndType}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>


                    <!-- Prep Time Column -->
                    <DataGridTextColumn Header="Prep Time" Binding="{Binding PrepTime}" IsReadOnly="True"/>

                    <!-- Start Time Column -->
                    <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime}" IsReadOnly="True"/>

                    <!-- End Time Column -->
                    <DataGridTextColumn Header="End Time" Binding="{Binding EndTime}" IsReadOnly="True"/>

                    <!-- Total Time Column -->
                    <DataGridTextColumn Header="Total Time" Binding="{Binding TotalTime}" IsReadOnly="True"/>

                    <!-- Machine Time Column -->
                    <DataGridTextColumn Header="Machine Time" Binding="{Binding MachineTime}" IsReadOnly="True"/>

                    <!-- Cut time column -->
                    <DataGridTemplateColumn Header="Cut Time" SortMemberPath="CutTime">
                        <DataGridTemplateColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Background" 
                                        Value="{Binding Converter={StaticResource CutTimeToBrushConverter}}" />
                            </Style>
                        </DataGridTemplateColumn.CellStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding CutTime}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>


                    <!-- Pause Time Column -->
                    <DataGridTemplateColumn Header="Pause Time" SortMemberPath="PauseTime">
                        <DataGridTemplateColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Background" 
                                        Value="{Binding Converter={StaticResource PauseTimeToBrushConverter}}" />
                            </Style>
                        </DataGridTemplateColumn.CellStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding PauseTime}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>



                    <!-- Wasted Time Column -->
                    <!-- <DataGridTextColumn Header="Wasted Time" Binding="{Binding WastedTime}" /> -->


                    <!-- Slew Time Column -->
                    <DataGridTextColumn Header="Slew Time" Binding="{Binding SlewTime}" IsReadOnly="True"/>

                    <!-- Sheet Change Time Column -->
                    <DataGridTextColumn Header="SC Time" Binding="{Binding SheetChangeTime}" IsReadOnly="True"/>

                    <!-- Tool Change Time Column -->
                    <DataGridTextColumn Header="TC Time" Binding="{Binding ToolChangeTime}" IsReadOnly="True"/>

                    <!-- Feedrate Override Column -->
                    <DataGridTextColumn Header="Feedrate Override" Binding="{Binding FeedrateOverride}" IsReadOnly="True"/>

                    <!-- PD Count Column -->
                    <DataGridTextColumn Header="PD Count" Binding="{Binding PD_Count}" IsReadOnly="True"/>

                    <!-- Material Column -->
                    <DataGridTextColumn Header="Material" Binding="{Binding Material}" IsReadOnly="True"/>

                    <!-- Sheet Count Column -->
                    <DataGridTextColumn Header="Sheet Count" Binding="{Binding SheetCount}" IsReadOnly="True"/>

                    <!-- Tools Column -->
                    <DataGridTextColumn Header="Tools" Binding="{Binding Tools}" IsReadOnly="True"/>
                    
                    <!-- Tool Avg Times Column -->
                    <DataGridTextColumn Header="Tool Avg Times" Binding="{Binding ToolAvgTimes}" IsReadOnly="True"/>

                    <!-- Job File Column -->
                    <DataGridTextColumn Header="Job File" Binding="{Binding JobFile}" IsReadOnly="True"/>

                    
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Side Panel for Selected Job Details -->
        <Expander Grid.Column="1" Grid.Row="0" Margin="10" IsExpanded="False" ExpandDirection="Left" MaxWidth="400">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <!-- Label column -->
                        <ColumnDefinition Width="*" />
                        <!-- Value column -->
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Labels and Values -->
                    <TextBlock Grid.Column="0" Grid.Row="0" FontWeight="Bold" Text="OEM String:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding SelectedJob.OEMString}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="1" FontWeight="Bold" Text="Connection:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding SelectedJob.Connection}" TextWrapping="Wrap" />
                    
                    <TextBlock Grid.Column="0" Grid.Row="2" FontWeight="Bold" Text="Shift:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding SelectedJob.Shift}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="3" FontWeight="Bold" Text="Job File:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="3" Text="{Binding SelectedJob.JobFile}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="4" FontWeight="Bold" Text="Start Type:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="4" Text="{Binding SelectedJob.StartType}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="5" FontWeight="Bold" Text="End Type:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="5" Text="{Binding SelectedJob.EndType}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="6" FontWeight="Bold" Text="Prep Time:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="6" Text="{Binding SelectedJob.PrepTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="7" FontWeight="Bold" Text="Start Time:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="7" Text="{Binding SelectedJob.StartTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="8" FontWeight="Bold" Text="End Time:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="8" Text="{Binding SelectedJob.EndTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="9" FontWeight="Bold" Text="Total Time:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="9" Text="{Binding SelectedJob.TotalTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="10" FontWeight="Bold" Text="Machine Time:" TextWrapping="Wrap" Margin="0,0,5,0"/>
                    <TextBlock Grid.Column="1" Grid.Row="10" Text="{Binding SelectedJob.MachineTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="11" FontWeight="Bold" Text="Cut Time:" TextWrapping="Wrap" Margin="0,0,5,0" Background="LightGreen"/>
                    <TextBlock Grid.Column="1" Grid.Row="11" Text="{Binding SelectedJob.CutTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="12" FontWeight="Bold" Text="Feedrate Override %:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="12" Text="{Binding SelectedJob.FeedrateOverride}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="13" FontWeight="Bold" Text="Slew Time:" TextWrapping="Wrap" Margin="0,0,5,0" Background="LightBlue"/>
                    <TextBlock Grid.Column="1" Grid.Row="13" Text="{Binding SelectedJob.SlewTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="14" FontWeight="Bold" Text="Pause Time:" TextWrapping="Wrap" Margin="0,0,5,0" Background="LightCoral"/>
                    <TextBlock Grid.Column="1" Grid.Row="14" Text="{Binding SelectedJob.PauseTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="15" FontWeight="Bold" Text="Sheet Change Time:" TextWrapping="Wrap" Margin="0,0,5,0" Background="Yellow"/>
                    <TextBlock Grid.Column="1" Grid.Row="15" Text="{Binding SelectedJob.SheetChangeTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="16" FontWeight="Bold" Text="Sheet Count:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="16" Text="{Binding SelectedJob.SheetCount}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="17" FontWeight="Bold" Text="Tool Change Time:" TextWrapping="Wrap" Margin="0,0,5,0" Background="Violet"/>
                    <TextBlock Grid.Column="1" Grid.Row="17" Text="{Binding SelectedJob.ToolChangeTime}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="18" FontWeight="Bold" Text="Tools:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="18" Text="{Binding SelectedJob.Tools}" TextWrapping="Wrap" />

                    <TextBlock Grid.Column="0" Grid.Row="19" FontWeight="Bold" Text="Tool Avg Times:" TextWrapping="Wrap" Margin="0,0,5,0" />
                    <TextBlock Grid.Column="1" Grid.Row="19" Text="{Binding SelectedJob.ToolAvgTimes}" TextWrapping="Wrap" />
                </Grid>

                <oxy:PlotView Model="{Binding SelectedJob.PieChartModel}" 
                      Height="300" 
                      Width="300" 
                      Margin="50" />
            </StackPanel>
        </Expander>



    </Grid>
</UserControl>